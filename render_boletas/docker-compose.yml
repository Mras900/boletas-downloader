version: "3.9"

services:
  boletas_app:
    build: .
    container_name: boletas_app
    restart: always

    ports:
      - "10000:10000"

    volumes:
      - ./output:/tmp/boletas_app

    environment:
      - TZ=America/Santiago